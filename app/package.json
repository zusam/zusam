{
	"name": "zusam-app",
	"version": "0.1.0",
	"scripts": {
		"build": "parcel build src/index.html",
		"serve": "shx cp dist/* ../public"
	},
	"browserslist": [
		"Chrome >= 60",
		"ChromeAndroid >= 60",
		"Edge >= 12",
		"Firefox >= 50",
		"FirefoxAndroid >= 50"
	],
	"dependencies": {
		"babel-preset-env": "^1.6.1",
		"babel-preset-hyperapp": "^2.3.0",
		"hyperapp": "^1.2.5",
		"parcel-bundler": "^1.7.0",
		"shx": "^0.2.2"
	}
}
